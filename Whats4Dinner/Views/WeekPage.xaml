<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:d="http://xamarin.com/schemas/2014/forms/design"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:vm="clr-namespace:Whats4Dinner.ViewModels"
			 mc:Ignorable="d"
			 x:Class="Whats4Dinner.Views.WeekPage"
			 Title="{Binding Title}">
	<ContentPage.BindingContext>
		<vm:WeekViewModel/>
	</ContentPage.BindingContext>

	<ContentPage.Resources>
		<ResourceDictionary>
			<Style x:Key="baseLabelStyle" TargetType="Label">
				<Setter Property="FontFamily" Value="Arial"/>
				<Setter Property="VerticalTextAlignment" Value="Center"/>
			</Style>
			<Style x:Key="ListItemTextStyle"
				   TargetType="Label"
				   BasedOn="{StaticResource baseLabelStyle}">
				<Setter Property="FontSize" Value="20"/>
			</Style>
			<Style x:Key="ListItemDetailTextStyle"
				   TargetType="Label"
				   BasedOn="{StaticResource baseLabelStyle}">
				<Setter Property="FontSize" Value="16"/>
				<Setter Property="Margin" Value="10, 0, 0, 0"/>
				<Setter Property="TextColor" Value="LightGray"/>
			</Style>
		</ResourceDictionary>
	</ContentPage.Resources>
	
	<ListView x:Name="Days"
			  HasUnevenRows="True"
			  VerticalOptions="FillAndExpand"
			  Margin="5, 5, 5, 5"
			  ItemsSource="{Binding DisplayDays}"
			  ItemTapped="Handle_ItemTapped"
			  CachingStrategy="RecycleElement">
		
		<ListView.ItemTemplate>
			<DataTemplate>
				<ViewCell>
					<StackLayout Margin="10, 8, 10, 7">
						<Grid>
							<Grid.RowDefinitions>
								<RowDefinition/>
								<RowDefinition/>
							</Grid.RowDefinitions>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="3*"/>
								<ColumnDefinition Width="2*"/>
							</Grid.ColumnDefinitions>
							
							<!-- Day of the week -->
							<Label Text="{Binding DisplayDayOfWeek}"
								   Grid.Row="0"
								   Grid.Column="0"
								   Style="{DynamicResource ListItemTextStyle}"/>
							
							<!-- exact date -->
							<Label Text="{Binding DisplayDate}"
								   Grid.Row="0"
								   Grid.Column="1"
								   Style="{DynamicResource ListItemTextStyle}"/>
							
							<!-- indicate whether each meal is added to day -->
							<Grid Grid.Row="1"
								  Grid.Column="0"
								  Grid.ColumnSpan="2">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="14*"/>
									<ColumnDefinition Width="10*"/>
									<ColumnDefinition Width="10*"/>
									<ColumnDefinition Width="10*"/>
								</Grid.ColumnDefinitions>
								<Label Text="Breakfast" 
									   Grid.Column="0" Style="{DynamicResource ListItemDetailTextStyle}"/>
								<Label Text="Breakfast" 
									   Grid.Column="0" Style="{DynamicResource ListItemDetailTextStyle}" TextColor="Black" IsVisible="{Binding BreakfastCheck}"/>
								<Label Text="Lunch" 
									   Grid.Column="1" Style="{DynamicResource ListItemDetailTextStyle}"/>
								<Label Text="Lunch" 
									   Grid.Column="1" Style="{DynamicResource ListItemDetailTextStyle}" TextColor="Black" IsVisible="{Binding LunchCheck}"/>
								<Label Text="Dinner" 
									   Grid.Column="2" Style="{DynamicResource ListItemDetailTextStyle}"/>
								<Label Text="Dinner" 
									   Grid.Column="2" Style="{DynamicResource ListItemDetailTextStyle}" TextColor="Black" IsVisible="{Binding DinnerCheck}"/>
								<Label Text="Other" 
									   Grid.Column="3" Style="{DynamicResource ListItemDetailTextStyle}"/>
								<Label Text="Other" 
									   Grid.Column="3" Style="{DynamicResource ListItemDetailTextStyle}" TextColor="Black" IsVisible="{Binding OtherCheck}"/>
							</Grid>
						</Grid>
					</StackLayout>
				</ViewCell>
			</DataTemplate>
		</ListView.ItemTemplate>
		
	</ListView>
</ContentPage>
